version = '0.6.0'
repository_id = 'niilun/tabs' # If you fork TABS, update this URL with your_username/repo so the update check works.

update_available = False

# Window holders
# These variables hold windows so the same button doesn't spawn more than one of each at once
stat_window_active = None